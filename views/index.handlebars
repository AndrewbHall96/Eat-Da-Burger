<body>

<div class="container">
<h1 class="header1">Eat-Da-Burger!</h1>

<h2>Ready to Eat</h2>

<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Eating Later</h2>

<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Order Here:</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devour">What would you like?</label><br>
    <input type="radio" name="devour" value="1" checked>Devour!<br>
    <input type="radio" name="devour" value="0"> Save 4 later!
  </div>

  <button type="submit">Make Burger</button>
</form>
</div>
</body>
